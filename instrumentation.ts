/**
 * Next.js instrumentation hook — runs once when the server process starts.
 * StampChain has no background jobs; this is intentionally a no-op.
 */
export async function register() {
  // no-op
}
